# 测试管理平台-测试智编模块业务逻辑流程图

> 版本：v0.1（创建于 2026-03-28）
> 关联文档：`测试管理平台-测试智编模块需求方案-20260327.md`、`测试管理平台-测试智编模块详细设计-20260328.md`、`测试管理平台-测试智编模块数据与接口设计-20260328.md`
> 文档定位：用于业务评审、研发评审、原型讲解和联调前对齐

## 1. 说明

本文档仅基于当前已经确认的需求输出业务逻辑流程图，不引入未确认的技术实现细节。

当前流程图覆盖以下范围：
- 任务创建到脚本确认的主业务流程
- 回放验证与脚本确认的前置关系
- 重新验证触发与状态回退逻辑

## 2. 主业务流程图

```mermaid
flowchart TD
    A["进入测试智编模块"] --> B["选择项目"]
    B --> C["选择测试用例（至少 1 条，可多选）"]
    C --> D["创建生成任务"]
    D --> E{"必填信息是否完整"}
    E -- "否" --> F["保存失败并提示缺失项"]
    F --> D
    E -- "是" --> G["任务进入 待执行"]
    G --> H["用户触发执行生成"]
    H --> I["任务进入 执行中"]
    I --> J["browser-use 执行页面探索"]
    J --> K["系统记录操作轨迹与执行证据"]
    K --> L{"探索与生成是否成功"}
    L -- "否" --> M["任务进入 生成失败"]
    M --> N["保留失败日志、错误原因、最后截图、已采集轨迹"]
    N --> O["用户可重新生成或复制配置新建任务"]
    L -- "是" --> P["生成 Playwright 脚本草稿"]
    P --> Q["生成基础断言与断言建议"]
    Q --> R["用户查看脚本、轨迹、截图、日志"]
    R --> S["用户手动点击 验证"]
    S --> T["进入回放验证流程"]
```

## 3. 回放验证与确认流程图

```mermaid
flowchart TD
    A["用户手动触发回放验证"] --> B["系统执行脚本回放"]
    B --> C["记录失败步骤、报错、截图、日志、断言结果"]
    C --> D{"回放验证结果"}
    D -- "验证通过" --> E["脚本状态进入 待确认"]
    E --> F{"当前用户是否具备确认权限"}
    F -- "否" --> G["仅可查看结果或提交待确认"]
    F -- "是" --> H["用户执行脚本确认"]
    H --> I["脚本状态变为 已确认"]
    I --> J["任务状态变为 已确认"]
    D -- "验证失败" --> K["脚本不可确认"]
    K --> L["任务 / 脚本状态进入 待重新验证"]
    L --> M["用户手动调试脚本"]
    M --> N["再次手动触发验证"]
    N --> B
    D -- "验证异常" --> O["本次验证不作为可确认依据"]
    O --> P["用户重新触发验证"]
    P --> B
```

## 4. 重新验证触发逻辑图

```mermaid
flowchart TD
    A["已有脚本版本"] --> B{"是否发生变更"}
    B -- "否" --> C["保持当前验证结果"]
    B -- "是" --> D{"变更类型"}
    D -- "重新生成脚本" --> E["必须重新验证"]
    D -- "人工编辑并保存新版本" --> E
    D -- "步骤 / 定位器 / 断言 / 等待逻辑变更" --> E
    D -- "起始地址 / 环境 / 账号 / 浏览器变更" --> E
    D -- "关联用例关键内容变更且影响脚本" --> E
    D -- "仅改任务名称 / 脚本名称 / 备注 / 标签" --> F["无需重新验证"]
    D -- "仅执行预览 / 复制 / 导出" --> F
    D -- "仅改展示类信息" --> F
    E --> G["原验证结果立即失效"]
    G --> H["脚本状态进入 待重新验证"]
    H --> I{"原脚本是否已确认"}
    I -- "否" --> J["等待用户重新验证"]
    I -- "是" --> K["生成新版本"]
    K --> L["新版本状态进入 待确认 + 待重新验证"]
    L --> M["用户重新触发验证"]
    J --> M
    F --> N["不改变验证状态"]
```

## 5. 流程解读

### 5.1 业务主线

- 测试智编任务必须先绑定项目和至少 1 条测试用例，才能进入执行生成阶段。
- 生成成功后，系统输出 `Playwright` 脚本草稿，并同步生成轨迹、证据、断言内容或断言建议。
- 脚本生成完成并不代表可以直接确认，必须先经过手动触发的回放验证。

### 5.2 确认前置关系

- 只有回放验证结果为“验证通过”的脚本，才允许进入确认。
- 回放验证失败时，脚本不能被确认，只能先调试再重新验证。
- `admin / manager / reviewer` 才能执行最终确认，`tester` 只能提交待确认。

### 5.3 重新验证逻辑

- 影响脚本行为或回放结果的变更，都会让原验证结果失效。
- 已确认脚本一旦发生关键变更，不是原地覆盖，而是生成新版本并重新走验证与确认流程。
- 不影响脚本执行结果的展示类变更，不触发重新验证。

## 6. 建议使用方式

这份流程图建议用于：
- 产品评审时讲解业务闭环
- 研发评审时统一状态与操作顺序
- 原型设计时核对按钮显隐和可点击条件
- 联调前核对任务、脚本、验证三类状态的边界

## 7. 后续可继续补充的图

如果后面还要继续细化，建议下一步补这 3 类图：
- 页面状态流转图
- 后端时序图
- 异常处理流程图

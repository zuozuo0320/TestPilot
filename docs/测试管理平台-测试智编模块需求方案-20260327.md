# 测试管理平台-测试智编模块需求方案（讨论稿）

> 版本：v0.1（创建于 2026-03-27）
> 项目：Aisight（后端 TestPilot / 前端 TestFront）
> 适用对象：产品、研发、测试、运维
> 文档定位：讨论稿，仅用于需求收敛，不代表最终开发方案

## 1. 模块目标

在测试管理平台内新增“测试智编”能力，用于把用户在真实页面上的业务场景探索过程，沉淀为可维护的自动化测试脚本。

本模块采用两阶段方案：
- `browser-use` 负责探索页面、执行场景、记录操作轨迹
- `Playwright / Cypress` 负责承载最终测试脚本、稳定回放、接入测试体系

模块目标不是直接替代现有自动化测试框架，而是降低测试脚本的编写门槛，提升从业务场景到自动化脚本的转换效率。

## 2. 模块定位

### 2.1 定位说明

本模块是测试管理平台中的独立功能模块，用于：
- 基于真实页面交互过程生成自动化脚本初稿
- 将业务人员、测试人员的场景操作沉淀为可复用测试资产
- 为后续自动化执行、回归测试、CI 集成提供脚本来源

### 2.2 与现有能力关系

- 本模块不替代“用例管理”，而是与“用例管理”配套
- 测试用例描述的是业务验证目标；测试脚本描述的是自动化执行实现
- 测试智编任务必须关联至少 1 条测试用例，且支持同时关联多条测试用例
- 本模块生成的是“脚本草稿/脚本资产”，不是最终的自动化执行引擎

### 2.3 核心原则

1. 先采集真实操作轨迹，再生成脚本
2. 先生成脚本草稿，再进入人工确认或平台内二次编辑
3. 操作轨迹、脚本草稿、正式脚本三个层次需清晰分离
4. AI 生成的脚本默认不视为“已验证通过”，必须具备确认状态

## 3. 业务范围

### 3.1 In Scope（范围内）

1. 创建智能脚本生成任务
2. 配置目标站点、场景描述、账号信息、执行环境
3. 使用 browser-use 执行页面探索与场景操作
4. 记录结构化操作轨迹
5. 基于操作轨迹生成 Playwright 或 Cypress 脚本
6. 将脚本与一条或多条测试用例建立关联
7. 保存脚本版本、生成时间、生成人、生成方式
8. 预览脚本、复制脚本、导出脚本
9. 记录生成日志、失败原因、关键截图与页面证据

### 3.2 Out of Scope（暂不纳入）

1. 平台内完整自动修复引擎
2. 平台内完整可视化脚本编辑器
3. 全量替代人工编写 Playwright/Cypress 测试
4. 所有网站场景下 100% 稳定生成可执行脚本
5. 自动生成高质量业务断言闭环而无需人工确认

## 4. 核心业务模型

### 4.1 对象定义

| 对象 | 说明 |
|------|------|
| 智能脚本生成任务 | 一次场景探索与脚本生成过程 |
| 操作轨迹 | browser-use 执行过程中的结构化动作记录 |
| 脚本草稿 | 基于操作轨迹生成的 Playwright/Cypress 代码初稿 |
| 正式脚本 | 经人工确认后沉淀的脚本版本 |
| 生成证据 | 包含截图、页面信息、日志、错误信息、关键 DOM/定位器候选 |

### 4.2 生命周期建议

智能脚本生成任务建议包含以下状态：
- `待配置`
- `待执行`
- `执行中`
- `生成成功`
- `生成失败`
- `待确认`
- `已确认`
- `已废弃`

脚本草稿建议包含以下状态：
- `草稿`
- `待确认`
- `已确认`
- `已废弃`

## 5. 主流程

### 5.1 标准流程

1. 用户选择项目与目标测试用例（至少 1 条，可多选）
2. 用户创建“智能脚本生成任务”
3. 用户填写场景描述、起始地址、账号信息、目标框架、附加说明
4. 平台调用 browser-use 执行场景探索
5. 系统记录结构化操作轨迹与执行证据
6. 系统根据轨迹生成 Playwright 或 Cypress 脚本草稿
7. 用户查看脚本草稿、轨迹、截图、日志
8. 用户确认脚本草稿，沉淀为正式脚本版本
9. 正式脚本与已关联测试用例保持关联，用于后续维护与执行

### 5.2 失败流程

1. 若探索阶段失败，任务状态置为 `生成失败`
2. 系统保留失败日志、错误原因、最后页面截图、已采集轨迹
3. 用户可基于原任务重新生成，或复制配置后新建任务

## 6. 关键业务规则

### 6.1 总体规则

1. 模块输出的核心结果是“自动化脚本草稿”，不是直接保证可用于生产回归的最终脚本
2. 脚本生成必须基于结构化操作轨迹，不允许只基于自然语言场景描述直接落最终脚本
3. 一次生成任务必须绑定所属项目
4. 生成任务必须关联至少 1 条测试用例，且支持关联多条测试用例
5. 同一测试用例允许存在多个脚本版本或多个框架脚本

### 6.2 探索与轨迹采集规则

1. browser-use 负责页面探索、执行场景、记录操作轨迹
2. 操作轨迹至少应记录：
   - 页面访问 URL
   - 动作类型（跳转、点击、输入、选择、上传、滚动、等待、断言候选等）
   - 目标元素信息
   - 候选定位器信息
   - 动作输入值
   - 动作执行结果
   - 时间顺序
3. 关键步骤需保留截图或页面证据
4. 失败步骤需保留错误信息与失败前最后状态
5. 敏感信息在轨迹、日志、脚本展示中应支持脱敏

### 6.3 脚本生成规则

1. 初期支持生成 `Playwright` 与 `Cypress` 两类脚本
2. 平台应允许用户在创建任务时选择目标输出框架
3. 若未特别指定，建议默认输出 `Playwright`
4. 生成脚本应尽量输出可维护代码，而非简单动作堆叠
5. 生成脚本时应优先使用更稳定的定位方式，候选优先级建议为：
   - `data-testid`
   - 语义化 role/label
   - 稳定文本
   - CSS/XPath 兜底
6. 若生成过程中无法获得可靠定位器，应在脚本或结果中明确标注风险
7. 生成脚本应包含必要注释，标识关键业务步骤与人工确认点

### 6.4 测试脚本定位规则

1. Playwright / Cypress 负责承载最终测试脚本、稳定回放、接入测试体系
2. 平台内生成的脚本默认归类为“脚本草稿”
3. 脚本草稿确认后，方可标记为“正式脚本”
4. 正式脚本应支持版本化管理
5. 脚本修改不影响原始操作轨迹留存
6. 首期允许在平台内对脚本草稿进行有限编辑，但不提供完整在线编辑器能力
7. 平台内编辑后的脚本必须保存为新版本，不覆盖原始 AI 生成稿
8. 已确认脚本若再次编辑，应生成新版本并回到 `待确认` 状态

### 6.5 与测试用例的关联规则

1. 一个测试智编任务必须关联至少 1 条测试用例，且支持同时关联多条测试用例
2. 一条测试用例可关联多个测试智编任务与多个脚本版本
3. 每个脚本需明确：
   - 所属项目
   - 关联用例
   - 输出框架
   - 版本号
   - 生成来源（AI 生成 / 人工修改 / 混合）
   - 生成人
   - 生成时间
4. 删除测试用例时，默认不物理删除其关联脚本，脚本进入“孤立脚本待处理”状态，具体策略待后续确认

### 6.6 人工确认规则

1. AI 生成结果必须支持人工确认
2. 未确认脚本不得默认纳入正式回归资产
3. 人工确认前，脚本状态为 `待确认`
4. 人工确认后，脚本状态变为 `已确认`
5. 若脚本确认后再次编辑，应形成新版本，不覆盖旧版本
6. `admin` / `manager` / `reviewer` 可执行脚本确认；`tester` 可提交脚本待确认，但不可执行最终确认

## 7. 功能需求（初稿）

### 7.1 任务管理

| 编号 | 需求点 | 优先级 |
|------|--------|--------|
| FR-AI-01 | 新建智能脚本生成任务 | P0 |
| FR-AI-02 | 选择目标输出框架（Playwright / Cypress） | P0 |
| FR-AI-03 | 配置场景描述、起始地址、账号信息、补充说明 | P0 |
| FR-AI-04 | 查看任务执行状态 | P0 |
| FR-AI-05 | 查看任务失败原因与日志 | P0 |
| FR-AI-06 | 复制已有任务配置重新生成 | P1 |
| FR-AI-07 | 查看历史生成任务列表 | P1 |

### 7.2 操作轨迹与证据

| 编号 | 需求点 | 优先级 |
|------|--------|--------|
| FR-AI-08 | 保存结构化操作轨迹 | P0 |
| FR-AI-09 | 保存关键截图与页面证据 | P0 |
| FR-AI-10 | 展示动作时间线 | P1 |
| FR-AI-11 | 展示候选定位器与最终采用定位器 | P1 |
| FR-AI-12 | 失败步骤保留错误详情 | P0 |

### 7.3 脚本生成与管理

| 编号 | 需求点 | 优先级 |
|------|--------|--------|
| FR-AI-13 | 基于轨迹生成 Playwright 脚本 | P0 |
| FR-AI-14 | 基于轨迹生成 Cypress 脚本 | P1 |
| FR-AI-15 | 生成脚本草稿并保存版本 | P0 |
| FR-AI-16 | 预览脚本内容 | P0 |
| FR-AI-17 | 复制脚本内容 | P0 |
| FR-AI-18 | 导出脚本文件 | P1 |
| FR-AI-19 | 标记脚本草稿为待确认 / 已确认 / 已废弃 | P0 |
| FR-AI-20 | 保存生成来源与生成元数据 | P1 |
| FR-AI-25 | 平台内支持脚本草稿有限编辑 | P1 |
| FR-AI-26 | 编辑后保存为新版本，不覆盖原始生成稿 | P1 |
| FR-AI-27 | 首期不提供完整在线编辑器能力 | P1 |
| FR-AI-28 | `admin` / `manager` / `reviewer` 可确认脚本，`tester` 仅可提交待确认 | P0 |

### 7.4 用例关联

| 编号 | 需求点 | 优先级 |
|------|--------|--------|
| FR-AI-21 | 创建任务时必须关联至少 1 条测试用例，支持多选 | P0 |
| FR-AI-22 | 生成后允许调整关联测试用例 | P1 |
| FR-AI-23 | 用例详情中展示关联脚本列表 | P0 |
| FR-AI-24 | 脚本列表中展示所属框架、状态、版本、更新时间 | P1 |

## 8. 建议页面结构

### 8.1 智能脚本生成任务列表

建议字段：
- 任务名称
- 所属项目
- 关联用例
- 输出框架
- 当前状态
- 生成人
- 生成时间
- 最近更新时间

### 8.2 任务详情页

建议分区：
- 基础信息
- 场景描述
- 执行日志
- 操作轨迹时间线
- 截图与证据
- 生成脚本
- 版本记录

### 8.3 用例详情中的脚本区域

建议字段：
- 脚本名称
- 框架类型
- 状态
- 版本号
- 生成来源
- 生成时间
- 操作入口（预览 / 复制 / 导出 / 继续生成）

## 9. 角色与权限建议（讨论版）

建议权限边界如下：
- `admin`：可查看、创建、删除、确认所有脚本生成任务
- `manager`：可查看、创建、确认本项目脚本生成任务
- `tester`：可查看、创建本项目脚本生成任务，可提交脚本待确认，但不可执行最终确认
- `reviewer`：可查看脚本结果，可执行脚本确认
- `developer`：默认只读，可按后续策略决定是否允许导出脚本
- `readonly`：仅查看，不允许创建或确认

> 上述权限为建议口径，需后续与你继续确认

## 10. 非功能要求建议

1. 任务执行过程需具备超时控制
2. 日志、截图、轨迹需可追溯
3. 敏感信息需脱敏存储与展示
4. 失败任务需可复盘
5. 脚本生成结果需支持复制与导出
6. 后续若接入执行能力，应与当前需求解耦设计

## 11. 当前已确认结论

1. 本能力作为测试管理平台中的独立模块/功能点集成
2. 总体技术路线采用两阶段方案：
   - `browser-use` 负责探索页面、执行场景、记录操作轨迹
   - `Playwright / Cypress` 负责承载最终测试脚本、稳定回放、接入测试体系
3. 已确认：测试智编必须绑定测试用例，且支持同时绑定多条测试用例
4. 已确认：首期允许平台内对脚本草稿进行有限编辑，但不提供完整在线编辑器能力
5. 已确认：`admin` / `manager` / `reviewer` 可确认脚本，`tester` 仅可提交待确认

## 12. 下一步建议讨论项

1. Playwright 是否作为默认且首期唯一输出
2. 是否支持脚本生成后二次回放验证
3. 断言生成策略是否纳入首期范围
4. 账号口令、验证码、MFA 场景如何定义边界


# 测试管理平台-测试智编模块数据与接口设计（初稿）

> 版本：v0.1（创建于 2026-03-28）
> 关联文档：`测试管理平台-测试智编模块需求方案-20260327.md`、`测试管理平台-测试智编模块详细设计-20260328.md`
> 适用对象：后端研发、前端研发、测试、架构
> 文档定位：用于后端表结构设计、接口联调、状态枚举统一与研发任务拆分

## 1. 设计说明

本文档在已确认需求与详细设计基础上，继续细化：
- 数据表与字段草案
- 状态枚举与业务字典
- API 入参出参建议
- 关键错误码与校验规则
- 前后端联调口径

说明：
- 字段类型采用通用关系型数据库表达，可按最终技术栈映射为 MySQL / PostgreSQL 实际类型
- 所有时间字段统一建议使用 `datetime` 或等价时区安全类型
- 所有主键示例统一使用 `bigint` 或平台既有雪花 ID 方案
- 首期唯一脚本输出框架固定为 `Playwright`

## 2. 枚举与字典

### 2.1 任务状态 task_status

| 值 | 说明 |
|----|------|
| DRAFT | 待配置 |
| PENDING_EXECUTE | 待执行 |
| RUNNING | 执行中 |
| GENERATE_SUCCESS | 生成成功 |
| GENERATE_FAILED | 生成失败 |
| PENDING_CONFIRM | 待确认 |
| PENDING_REVALIDATE | 待重新验证 |
| CONFIRMED | 已确认 |
| DISCARDED | 已废弃 |

### 2.2 脚本状态 script_status

| 值 | 说明 |
|----|------|
| DRAFT | 草稿 |
| PENDING_CONFIRM | 待确认 |
| PENDING_REVALIDATE | 待重新验证 |
| CONFIRMED | 已确认 |
| DISCARDED | 已废弃 |

### 2.3 回放验证状态 validation_status

| 值 | 说明 |
|----|------|
| NOT_VALIDATED | 未验证 |
| VALIDATING | 验证中 |
| PASSED | 验证通过 |
| FAILED | 验证失败 |
| ERROR | 验证异常 |

### 2.4 生成来源 source_type

| 值 | 说明 |
|----|------|
| AI_GENERATED | AI 生成 |
| HUMAN_EDITED | 人工修改 |
| MIXED | 混合 |

### 2.5 回放触发方式 validation_trigger_type

| 值 | 说明 |
|----|------|
| MANUAL | 手动触发 |

### 2.6 轨迹动作类型 trace_action_type

| 值 | 说明 |
|----|------|
| NAVIGATE | 页面跳转 |
| CLICK | 点击 |
| INPUT | 输入 |
| SELECT | 选择 |
| UPLOAD | 上传 |
| SCROLL | 滚动 |
| WAIT | 等待 |
| ASSERT_CANDIDATE | 断言候选 |
| CUSTOM | 自定义扩展动作 |

### 2.7 日志级别 log_level

| 值 | 说明 |
|----|------|
| INFO | 普通信息 |
| WARN | 警告 |
| ERROR | 错误 |

## 3. 数据表草案

### 3.1 ai_script_task

说明：生成任务主表。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | bigint | 是 | 主键 |
| project_id | bigint | 是 | 所属项目 ID |
| task_name | varchar(128) | 是 | 任务名称 |
| scenario_desc | text | 是 | 场景描述 |
| start_url | varchar(512) | 是 | 起始地址 |
| account_ref | varchar(256) | 否 | 账号信息引用或展示值 |
| env_config_json | json | 否 | 执行环境配置 |
| task_status | varchar(32) | 是 | 任务状态 |
| framework_type | varchar(32) | 是 | 首期固定 `Playwright` |
| current_script_version_id | bigint | 否 | 当前脚本版本 ID |
| latest_validation_id | bigint | 否 | 最近一次回放验证 ID |
| latest_validation_status | varchar(32) | 否 | 最近一次回放验证状态 |
| latest_execute_at | datetime | 否 | 最近一次执行生成时间 |
| latest_confirmed_at | datetime | 否 | 最近一次确认时间 |
| latest_confirmed_by | bigint | 否 | 最近一次确认人 |
| discard_reason | varchar(255) | 否 | 废弃原因 |
| created_by | bigint | 是 | 创建人 |
| created_at | datetime | 是 | 创建时间 |
| updated_by | bigint | 是 | 更新人 |
| updated_at | datetime | 是 | 更新时间 |
| deleted_flag | tinyint | 是 | 逻辑删除标识，0/1 |

建议索引：
- `idx_ai_script_task_project_status(project_id, task_status)`
- `idx_ai_script_task_creator(created_by)`
- `idx_ai_script_task_updated_at(updated_at)`

### 3.2 ai_script_task_case_rel

说明：任务与测试用例多对多关系表。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | bigint | 是 | 主键 |
| task_id | bigint | 是 | 任务 ID |
| case_id | bigint | 是 | 用例 ID |
| created_by | bigint | 是 | 创建人 |
| created_at | datetime | 是 | 创建时间 |

约束建议：
- 唯一索引 `uk_task_case(task_id, case_id)`
- 创建任务时至少写入 1 条关系记录

### 3.3 ai_script_version

说明：脚本版本表，所有生成和人工编辑都新增版本，不覆盖原版本。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | bigint | 是 | 主键 |
| task_id | bigint | 是 | 所属任务 ID |
| version_no | int | 是 | 版本号，从 1 开始递增 |
| framework_type | varchar(32) | 是 | 首期固定 `Playwright` |
| script_name | varchar(128) | 否 | 脚本显示名称 |
| script_status | varchar(32) | 是 | 脚本状态 |
| validation_status | varchar(32) | 是 | 验证状态 |
| source_type | varchar(32) | 是 | 生成来源 |
| script_content | longtext | 是 | 脚本正文 |
| comment_text | varchar(500) | 否 | 本版本说明 |
| based_on_version_id | bigint | 否 | 基于哪个版本生成 |
| is_current_flag | tinyint | 是 | 是否当前版本，0/1 |
| latest_validation_id | bigint | 否 | 最近一次验证记录 |
| confirmed_by | bigint | 否 | 确认人 |
| confirmed_at | datetime | 否 | 确认时间 |
| created_by | bigint | 是 | 版本生成人 |
| created_at | datetime | 是 | 生成时间 |
| updated_by | bigint | 是 | 更新人 |
| updated_at | datetime | 是 | 更新时间 |
| deleted_flag | tinyint | 是 | 逻辑删除标识 |

建议索引：
- `uk_task_version(task_id, version_no)`
- `idx_script_current(task_id, is_current_flag)`
- `idx_script_status(task_id, script_status, validation_status)`

### 3.4 ai_script_validation

说明：脚本回放验证记录表。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | bigint | 是 | 主键 |
| script_version_id | bigint | 是 | 脚本版本 ID |
| task_id | bigint | 是 | 任务 ID，便于查询 |
| trigger_type | varchar(32) | 是 | 首期固定 `MANUAL` |
| validation_status | varchar(32) | 是 | 验证状态 |
| total_step_count | int | 否 | 总步骤数 |
| passed_step_count | int | 否 | 通过步骤数 |
| failed_step_no | int | 否 | 失败步骤序号 |
| fail_reason | text | 否 | 失败原因 |
| assertion_summary_json | json | 否 | 断言结果摘要 |
| started_at | datetime | 是 | 开始时间 |
| finished_at | datetime | 否 | 结束时间 |
| duration_ms | bigint | 否 | 耗时 |
| triggered_by | bigint | 是 | 触发人 |
| created_at | datetime | 是 | 创建时间 |

建议索引：
- `idx_validation_script(script_version_id, created_at desc)`
- `idx_validation_task(task_id, created_at desc)`

### 3.5 ai_script_trace

说明：结构化轨迹表。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | bigint | 是 | 主键 |
| task_id | bigint | 是 | 任务 ID |
| trace_no | int | 是 | 轨迹序号 |
| action_type | varchar(32) | 是 | 动作类型 |
| page_url | varchar(1024) | 否 | 当前页面 URL |
| target_summary | varchar(512) | 否 | 目标元素摘要 |
| locator_used | varchar(1024) | 否 | 最终采用定位器 |
| locator_candidates_json | json | 否 | 候选定位器 |
| input_value_masked | text | 否 | 已脱敏输入值 |
| action_result | varchar(64) | 否 | 动作结果 |
| error_message | text | 否 | 错误信息 |
| screenshot_id | bigint | 否 | 关联截图 ID |
| raw_payload_json | json | 否 | 原始轨迹扩展信息 |
| occurred_at | datetime | 是 | 发生时间 |
| created_at | datetime | 是 | 创建时间 |

约束建议：
- 唯一索引 `uk_task_trace_no(task_id, trace_no)`

### 3.6 ai_script_evidence

说明：截图、日志附件、页面证据表。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | bigint | 是 | 主键 |
| task_id | bigint | 是 | 任务 ID |
| script_version_id | bigint | 否 | 脚本版本 ID |
| validation_id | bigint | 否 | 验证记录 ID |
| evidence_type | varchar(32) | 是 | SCREENSHOT / LOG / DOM / OTHER |
| file_name | varchar(255) | 否 | 文件名 |
| file_url | varchar(1024) | 否 | 存储地址 |
| content_text | longtext | 否 | 文本类证据内容 |
| trace_no | int | 否 | 关联轨迹序号 |
| created_by | bigint | 否 | 生成人 |
| created_at | datetime | 是 | 创建时间 |

### 3.7 ai_script_operation_log

说明：模块级操作审计日志。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | bigint | 是 | 主键 |
| task_id | bigint | 否 | 任务 ID |
| script_version_id | bigint | 否 | 脚本版本 ID |
| operation_type | varchar(64) | 是 | CREATE_TASK / EXECUTE / EDIT / VALIDATE / CONFIRM / DISCARD / EXPORT |
| operator_id | bigint | 是 | 操作人 |
| operator_name | varchar(128) | 否 | 操作人名称快照 |
| operation_desc | varchar(500) | 否 | 操作说明 |
| before_snapshot_json | json | 否 | 变更前快照 |
| after_snapshot_json | json | 否 | 变更后快照 |
| created_at | datetime | 是 | 创建时间 |

## 4. 关键约束与联动规则

### 4.1 创建任务校验

必须满足：
- `project_id` 必填
- 至少关联 1 条测试用例
- `task_name` 必填
- `scenario_desc` 必填
- `start_url` 必填
- `framework_type` 首期固定为 `Playwright`

### 4.2 生成成功后的落库顺序

建议顺序：
1. 写入或更新 `ai_script_task`
2. 写入 `ai_script_trace`
3. 写入 `ai_script_evidence`
4. 写入 `ai_script_version`
5. 回写任务当前脚本版本与状态
6. 写入操作日志

### 4.3 编辑脚本后的联动

保存为新版本时必须：
- `version_no + 1`
- 原版本保留，不覆盖
- 新版本 `script_status = PENDING_CONFIRM`
- 新版本 `validation_status = NOT_VALIDATED`
- 页面展示状态同步为 `待重新验证`
- 任务状态同步为 `PENDING_REVALIDATE`

### 4.4 确认脚本前校验

确认接口必须校验：
- 当前用户角色为 `admin / manager / reviewer`
- 当前脚本版本不为 `DISCARDED`
- 当前脚本版本 `validation_status = PASSED`
- 当前脚本版本为任务当前版本

### 4.5 重新验证触发后联动

命中重验场景时必须：
- 当前脚本版本 `validation_status` 置为 `NOT_VALIDATED`
- 当前脚本版本 `script_status` 置为 `PENDING_REVALIDATE`
- 任务状态置为 `PENDING_REVALIDATE`
- 原验证记录保留，不物理删除

## 5. API 统一约定

### 5.1 统一响应结构

建议统一响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

分页结构建议：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [],
    "page_no": 1,
    "page_size": 20,
    "total": 0
  }
}
```

### 5.2 通用错误码建议

| 错误码 | 说明 |
|--------|------|
| AI_SCRIPT_4001 | 参数校验失败 |
| AI_SCRIPT_4002 | 未关联测试用例 |
| AI_SCRIPT_4003 | 任务不存在 |
| AI_SCRIPT_4004 | 脚本版本不存在 |
| AI_SCRIPT_4005 | 当前状态不允许执行该操作 |
| AI_SCRIPT_4006 | 当前用户无权限 |
| AI_SCRIPT_4007 | 脚本尚未验证通过，不允许确认 |
| AI_SCRIPT_4008 | 当前版本不是任务主版本 |
| AI_SCRIPT_4009 | 验证中，不允许重复触发 |
| AI_SCRIPT_4010 | 已废弃版本不允许操作 |
| AI_SCRIPT_5001 | 生成任务执行失败 |
| AI_SCRIPT_5002 | 回放验证执行异常 |

## 6. API 详细定义

### 6.1 POST /api/ai-script/tasks

说明：新建生成任务。

请求体：

```json
{
  "project_id": 1001,
  "task_name": "登录并进入用户列表",
  "case_ids": [2001, 2002],
  "scenario_desc": "使用测试账号登录系统并进入用户列表页",
  "start_url": "https://test.example.com/login",
  "account_ref": "test_user_a",
  "env_config": {
    "browser": "chromium",
    "env_code": "test",
    "timeout_sec": 120
  },
  "remark": "首期验证登录基础流程"
}
```

响应体：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "task_id": 9001,
    "task_status": "PENDING_EXECUTE"
  }
}
```

### 6.2 GET /api/ai-script/tasks

说明：获取任务列表。

请求参数建议：
- `project_id`
- `task_status`
- `validation_status`
- `created_by`
- `keyword`
- `page_no`
- `page_size`

列表项示例：

```json
{
  "task_id": 9001,
  "task_name": "登录并进入用户列表",
  "project_id": 1001,
  "project_name": "Aisight Web",
  "case_count": 2,
  "framework_type": "Playwright",
  "task_status": "PENDING_CONFIRM",
  "current_version_no": 3,
  "validation_status": "PASSED",
  "created_by": 3001,
  "created_name": "tester_a",
  "created_at": "2026-03-28 16:10:00",
  "updated_at": "2026-03-28 16:22:00"
}
```

### 6.3 GET /api/ai-script/tasks/{taskId}

说明：获取任务详情。

返回建议包含：
- 任务基础信息
- 关联用例列表
- 当前脚本摘要
- 最近一次验证摘要
- 允许操作按钮权限标识

### 6.4 POST /api/ai-script/tasks/{taskId}/execute

说明：执行生成任务。

请求体建议：

```json
{
  "force_regenerate": false
}
```

返回建议：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "task_id": 9001,
    "task_status": "RUNNING"
  }
}
```

### 6.5 POST /api/ai-script/tasks/{taskId}/clone

说明：复制任务配置生成新任务。

请求体建议：

```json
{
  "task_name": "登录并进入用户列表-副本"
}
```

### 6.6 POST /api/ai-script/tasks/{taskId}/discard

说明：废弃任务。

请求体建议：

```json
{
  "reason": "需求已取消"
}
```

约束：
- 已废弃任务不可重复废弃
- 废弃任务不物理删除历史轨迹、脚本、验证记录

### 6.7 GET /api/ai-script/tasks/{taskId}/scripts/current

说明：获取当前脚本版本详情。

返回建议包含：
- 脚本版本基础信息
- 脚本内容
- 状态
- 验证状态
- 风险提示
- 断言建议列表

### 6.8 GET /api/ai-script/tasks/{taskId}/scripts/versions

说明：获取版本列表。

列表项建议字段：
- `script_version_id`
- `version_no`
- `script_status`
- `validation_status`
- `source_type`
- `created_by`
- `created_at`
- `is_current`

### 6.9 POST /api/ai-script/scripts/{scriptId}/edit

说明：有限编辑脚本并生成新版本。

请求体：

```json
{
  "script_content": "test('登录并进入用户列表', async ({ page }) => { /* ... */ })",
  "comment_text": "补充了等待逻辑和提示语断言"
}
```

处理结果要求：
- 创建新版本
- 原版本保留
- 新版本状态置为 `PENDING_CONFIRM`
- 新版本验证状态置为 `NOT_VALIDATED`

### 6.10 POST /api/ai-script/scripts/{scriptId}/confirm

说明：确认脚本。

请求体建议：

```json
{
  "comment_text": "验证通过，准入正式脚本"
}
```

确认失败条件：
- 非审核角色调用
- 验证状态非 `PASSED`
- 当前版本不是主版本
- 当前版本已废弃

### 6.11 POST /api/ai-script/scripts/{scriptId}/discard

说明：废弃脚本版本。

请求体建议：

```json
{
  "reason": "定位器策略已弃用"
}
```

### 6.12 GET /api/ai-script/scripts/{scriptId}/export

说明：导出脚本文件。

响应建议：
- 文件流下载
- 或返回带时效签名的下载地址

### 6.13 POST /api/ai-script/scripts/{scriptId}/validate

说明：手动触发回放验证。

请求体建议：

```json
{
  "remark": "人工调试后重新验证"
}
```

处理约束：
- 同一脚本版本 `VALIDATING` 状态下禁止重复触发
- 首期仅支持手动触发
- 触发后立即创建一条 `ai_script_validation` 记录

返回示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "validation_id": 8001,
    "validation_status": "VALIDATING"
  }
}
```

### 6.14 GET /api/ai-script/scripts/{scriptId}/validations/latest

说明：获取最近一次验证结果。

返回建议字段：
- `validation_id`
- `validation_status`
- `total_step_count`
- `passed_step_count`
- `failed_step_no`
- `fail_reason`
- `assertion_summary`
- `triggered_by`
- `started_at`
- `finished_at`
- `duration_ms`
- `screenshot_list`
- `log_list`

### 6.15 GET /api/ai-script/scripts/{scriptId}/validations

说明：获取验证历史。

### 6.16 GET /api/ai-script/tasks/{taskId}/traces

说明：获取任务轨迹时间线。

返回建议字段：
- `trace_no`
- `action_type`
- `page_url`
- `target_summary`
- `locator_used`
- `action_result`
- `error_message`
- `occurred_at`
- `screenshot_url`

### 6.17 GET /api/ai-script/tasks/{taskId}/evidences

说明：获取任务证据列表。

### 6.18 GET /api/cases/{caseId}/scripts

说明：获取用例详情中的关联脚本列表。

### 6.19 POST /api/ai-script/tasks/{taskId}/cases/update

说明：更新任务关联用例。

请求体建议：

```json
{
  "case_ids": [2001, 2002, 2003]
}
```

约束：
- 更新后仍必须至少保留 1 条用例
- 若关联用例关键内容变更并影响脚本，需触发重新验证逻辑

## 7. 前后端联调口径

### 7.1 按钮显示与可点击分离

建议接口同时返回：
- `can_execute`
- `can_validate`
- `can_edit`
- `can_confirm`
- `can_export`
- `can_discard`

目的：
- 避免前端重复拼装复杂权限 + 状态判断
- 保持页面行为与后端真实规则一致

### 7.2 状态展示建议

前端展示建议至少保留两组状态：
- 任务状态
- 验证状态

不要只用一个状态字段替代全部页面含义，否则容易混淆“待确认”和“待重新验证”。

### 7.3 长任务执行建议

生成任务执行与回放验证都属于长任务，建议：
- 提交接口快速返回受理结果
- 前端通过轮询详情接口或独立状态接口刷新
- 后续若平台已有消息推送机制，可再升级为事件推送

## 8. 待后续补充但不阻塞首期开发的内容

以下内容可放入下一轮细化，不阻塞首期落地：
- 更细的对象存储目录规划
- 版本差异对比算法
- 断言建议的数据结构进一步标准化
- 执行环境对象的独立管理
- 与后续 CI / 执行中心的对接字段

## 9. 下一步建议

基于本文档，后续最适合继续推进的 2 份产物是：
1. `数据库表结构 SQL 草案`
2. `OpenAPI / Swagger 风格接口文档`
